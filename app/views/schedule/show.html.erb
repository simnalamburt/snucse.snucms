<i class="close icon"></i>
<div class="header">
  Schedule
</div>
<div class="content">
  <p>Course: <%= @schedule.course.name %></p>
  <p>Name: <%= @schedule.name %></p>
  <p>Due: <%= @schedule.due_date.strftime('%Y-%m-%d') %></p>
  <p>
    <%= simple_format(@schedule.content) %>
  </p>
  <div class="actions">
    <div class="ui hide-modal button">Close</div>
  </div>
</div>
